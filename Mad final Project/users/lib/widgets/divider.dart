import 'package:flutter/material.dart';

class DividerWidget extends StatelessWidget {

// Divider line in map page

  @override
  Widget build(BuildContext context) {
    return const Divider(
      height: 1.0,
      color: Colors.black,
      thickness: 1.0,
    );
  }
}